cd: cd [-] [dir]
    Change the shell working directory.    
    
    Change the current directory to DIR. The default DIR is the value of the HOME shell variable.
    If the directory is not found, and the shell option `cdable_vars' is set, the word is assumed to be a variable name. If that variable has a value, its value is used for DIR.
    
    Options:
      (-)     redirect the parrent working directory to the old parent working directory an environnment variable OLDPWD

    
    Exit Status:
    Returns 0 if the directory is changed, and if $PWD is set successfully, negtive number otherwise.
